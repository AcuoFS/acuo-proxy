'use strict'

const _ = require('lodash')
const Env = require('app/env')

// get env from Env, default is dev
const env = _.get(Env, 'env', 'dev')

// load current conf
const Conf = require(`./${env}`)


module.exports = (key) => key? _.get(Conf, key, {}): Conf